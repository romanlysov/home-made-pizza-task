import styled from "styled-components";
import {deliveryLabel} from "./images";

export const StyledDelivery = styled.div`
	width: 286px;
	height: 53px;
	background: url(${deliveryLabel}) center no-repeat;
	`;