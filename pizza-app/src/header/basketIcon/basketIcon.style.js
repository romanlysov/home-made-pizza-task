import styled from "styled-components";
import {basketImage} from "../images";

export const StyledBasketIcon = styled.div`
	width: 130px;
	height: 130px;
	background: url(${basketImage}) center no-repeat;
	background-size: 120px 120px;
`;
